{
  "name": "leadingstock-mobile",
  "version": "1.0.0",
  "description": "주도주 탐색기 모바일 앱",
  "scripts": {
    "build": "echo 'No build step needed for static files'",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.5.0",
    "@capacitor/android": "^5.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0"
  }
}

